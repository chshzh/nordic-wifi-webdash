# Sample Wi-Fi credential overlay for nordic_wifi_softap_webserver
#
# Copy this file to overlay-wifi-credentials.conf (listed in .gitignore)
# and replace the placeholder values with your production credentials.
# Do NOT commit the real overlay file.

CONFIG_APP_WIFI_SSID="YourWIFISSID"
CONFIG_APP_WIFI_PASSWORD="YourStrongPassword123!"
